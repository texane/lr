. step 2 must be adaptive
 . create more sublists per thread, enable workstealing
  . sublists are created and a workqueue wraps them
   . the algorithm becomes:
   while ((sublist = pop(sublists))) { subrank(sublist); }